import {Layout} from "../Layout";

export const UsersPage = (props) => {
    return (
        <Layout>
            <div>
                <h1>Пользователи</h1>
            </div>
        </Layout>
    )
}
